-- | Tagged integers, constructor and destructor are hidden.
-- This provides some type safety by making it impossible to mix different variable types.
module Util.Tagged.Var (TaggedVar) where

import Util.Tagged.Internal

